
#ifndef TWOCOLOR_PARTITIONONCE_H
#define TWOCOLOR_PARTITIONONCE_H
#include "../general.h"

namespace twoClr {

    // Solves the puzzle by separating the tiles labeled 1 to the top.
    std::string partitionOnce(std::vector<int>& boardState);

}

#endif //TWOCOLOR_PARTITIONONCE_H
